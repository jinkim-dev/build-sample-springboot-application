spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    data: classpath:data.sql
jenkins:
  url: http://localhost:8090/jenkins
  id: admin
  password: admin
server:
  servlet:
    context-path: /jindev